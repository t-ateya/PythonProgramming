"""
    built-in
    -iter, reversed, next, len, slicing, zip, filter, sorted, enumerate, min, max, sum, all, any, map
        functions module

    -
    
    The itertools module                             function name
    slicing                                          islice
    selecting and Filtering                             dropwhile   takewhile  compress filterfalse

    Changing and Teeing                              chain        tee
    Mapping and Reducing                             starmap, accumulate
    infinite iterators                               count, cycle, repeat
    zipping                                           zip_longest
    combinatorics                                     product          permutations
                                                    combinations, combinations_with_replacement
"""