
"""
    It is data structure that keeps track of elements which are partitioned into a number of disjoint
    and non overlapping sets and each sets have representative which helps in identifying that sets
        - Make Set 
        - Union
        - Find Set

    makeSet(N):used to create an initial Set
    union(x,y): merge two given sets
    findSet(x): returns the set name in which this element is there
"""